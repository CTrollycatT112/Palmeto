[package]
name = "drivers"
version.workspace = true
edition.workspace = true

[dependencies]
shared = { path = "../shared" }
kernel = { path = "../kernel" }

spin = { workspace = true }
fdt  = { workspace = true }
