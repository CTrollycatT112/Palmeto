[package]
name = "kernel"
version = "0.1.0"
edition = "2024"

[dependencies]
limine = { workspace = true }
flanterm = { workspace = true }

shared = { path = "../shared" }

[features]
default = ["qemu"]
potato = []
qemu = []