[workspace]
members = [
    "kernel",
    "drivers",
    "shared",
    "boot",
]
resolver = "2"

[profile.dev]
panic = "abort"
opt-level = 1

[profile.release]
panic = "abort"
lto = true
codegen-units = 1
strip = "debuginfo"
